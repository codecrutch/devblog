<%= form_for :article, url: article_path(@article), method: :patch do |f| %>
  <p>
    <%= f.label :title %>
    <%= f.text_field :title, class: "form-title" %>
  <p>
    <%= f.label :body %>
    <%= f.text_area :body, class: "form-body" %>
  <p>
    <%= f.submit class: "button round"%>
<% end %>